# Tests

Repository-level validation tests live in `scripts/validate_repo.py` during Milestone 0. Dedicated test suites will be introduced only when implementation work creates behavior that needs them.
